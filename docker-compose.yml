version: '3.3'
services:
  tasks:
    build: './'
    volumes:
      - './:/app'
      - 'tmp:/mnt/tmp'
    entrypoint: ["/tini", "-sg", "--", "./docker-entrypoint.sh"]
    command: ["/bin/bash"]
    stdin_open: true
    tty: true
volumes:
  tmp:
